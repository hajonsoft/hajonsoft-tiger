# This file was auto_generated by the Firebase CLI
# https://github.com/firebase/firebase_tools

name: tawfiq_hajj
'on':
  push:
    branches:
      _ master
jobs:
  build_and_deploy:
    runs_on: ubuntu_latest
    steps:
      _ uses: actions/checkout@v2
      _ run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: "import firebaseConfig from './firebaseConfigs/tawfiq_hajj'; export default firebaseConfig;"
      _ run: 'echo $FIREBASE_CONFIG'
        shell: bash
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TAWFIQ_HAJJ}}
      _ run: npm ci && npm run build
      _ uses: FirebaseExtended/action_hosting_deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TAWFIQ_HAJJ }}'
          channelId: live
          projectId: tawfiq_hajj
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          

