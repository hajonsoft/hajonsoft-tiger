# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: cashlessplus-2021
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: "import firebaseConfig from './firebaseConfigs/cashlessplus-2021'; export default firebaseConfig;"
      - run: 'echo $FIREBASE_CONFIG'
        shell: bash
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CASHLESSPLUS_2021}}
      - uses: nick-invision/retry@v2
        with:
          max_attempts: 3
          timeout_minutes: 10
          command: npm i --legacy-peer-deps && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CASHLESSPLUS_2021 }}'
          channelId: live
          projectId: cashlessplus-2021
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          

