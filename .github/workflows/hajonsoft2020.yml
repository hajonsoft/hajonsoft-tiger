# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: hajonsoft2020
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: 'export const firebaseConfig = { apiKey: "AIzaSyC9qB4VUiNl4GuI2_ABEJ8KPA71rBV2tZw", authDomain: "hajonsoft2020.firebaseapp.com", databaseURL: "https://hajonsoft2020-default-rtdb.firebaseio.com", projectId: "hajonsoft2020", storageBucket: "hajonsoft2020.appspot.com", messagingSenderId: "309798473169", appId: "1:309798473169:web:a7e58104d84b8e08d0a4f0" };'
      - run: 'echo $FIREBASE_CONFIG'
        shell: bash
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HAJONSOFT2020}}
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_HAJONSOFT2020 }}'
          channelId: live
          projectId: hajonsoft2020
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
