# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: hajj-mission-cote-ivoire
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: 'import firebaseConfig from './firebaseConfigs/hajj-mission-of-cote-de-ivoir'; export default firebaseConfig;'
      - run: 'echo $FIREBASE_CONFIG'
        shell: bash
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HAJJ_MISSION_OF_COTE_DE_IVOIR}}
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: >-
            ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HAJJ_MISSION_OF_COTE_DE_IVOIR
            }}
          channelId: live
          projectId: hajj-mission-of-cote-de-ivoir
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
