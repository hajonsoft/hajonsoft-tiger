# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: alhamdou-hajj2022-1c3b7
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: "import firebaseConfig from './firebaseConfigs/alhamdou-hajj2022-1c3b7'; export default firebaseConfig;"
      - run: 'echo $FIREBASE_CONFIG'
        shell: bash
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ALHAMDOU_HAJJ2022_1C3B7}}
      - uses: nick-invision/retry@v2
        with:
          max_attempts: 3
          timeout_minutes: 10
          command: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ALHAMDOU_HAJJ2022_1C3B7 }}'
          channelId: live
          projectId: alhamdou-hajj2022-1c3b7
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          

