# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: darelsalam-usa
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: checkout and configure for darelsalam-usa
      - run: 'echo  "$EXPORT_FIREBASE_CONFIG" > src/firebaseConfig.js'
        shell: bash
        env:
          EXPORT_FIREBASE_CONFIG: "import firebaseConfig from './firebaseConfigs/darelsalam-usa'; export default firebaseConfig;"
      - run: 'echo $FIREBASE_CONFIG'
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DARELSALAM_USA}}
      - uses: nick-invision/retry@v2
        with:
          max_attempts: 3
          timeout_minutes: 10
          command: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DARELSALAM_USA }}'
          channelId: live
          projectId: darelsalam-usa
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          

